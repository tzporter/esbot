[tool.ruff]
line-length = 88
select = ["E", "F", "W"]
exclude = [".venv", "__pycache__", "migrations"]

[tool.ruff.per-file-ignores]
"features/**/*.py" = ["F811"]

[tool.bandit]
exclude_dirs = ["tests", ".venv", "features"]
skips = []