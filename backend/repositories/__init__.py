# Even though this file is not necessarily required in this project
# and everything works successfully without this file, it has been added in order to:
# 1. Emphasize explicitly that this is a python package
# 2. Guarantee import across different environments (local, Docker, CI),
#    independent of the current working directory or sys.path setup
# 3. Avoid edge cases with tools that do not fully support namespace
#    packages (e.g. some linters, type checkers like mypy, packaging tools)
# 4. Keep the structure future-proof, so additional repositories added under
#    this package later are recognized consistently